<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kimje.springboard.user.repository.UserRepository">
    <insert id="save" parameterType="com.kimje.springboard.user.model.User">
        INSERT INTO users(
            email, nickname, picture_url, provider, provider_id, role
        ) VALUES (
                  #{email},
                  #{nickname},
                  #{pictureUrl},
                  #{provider},
                  #{providerId},
                  #{role}
                         )
    </insert>

    <select id="findByProviderAndProviderId" resultType="com.kimje.springboard.user.model.User">
        SELECT
            id, email, nickname, picture_url, role, provider, provider_id, created_at
        FROM
            users
        WHERE
            provider = #{provider} AND provider_id = #{providerId}

    </select>
    
</mapper> 